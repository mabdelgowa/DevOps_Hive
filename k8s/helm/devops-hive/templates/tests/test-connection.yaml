apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-hive.fullname" . }}-test-connection"
  labels:
    {{- include "devops-hive.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-hive.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
